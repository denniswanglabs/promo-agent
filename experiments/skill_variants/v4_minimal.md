---
name: promo
description: Triggered by requests to make/create/generate a promo video for a URL. The skill is a single shell command.
---

Run: `openclaw:core:exec` with `command = "bash /sandbox/.openclaw/skills/promo/scripts/make_promo.sh <THE_URL>"`. Wait ~2 min. Report stdout.
